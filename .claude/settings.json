{
  "hooks": {
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "cd \"/Users/lucaspit/Agentes Criadores de conteúdo\" && if [ -n \"$(git status --porcelain)\" ]; then git add -A && git commit -m \"auto: atualizações do projeto $(date '+%Y-%m-%d %H:%M')\" && git push origin main; fi",
            "timeout": 30,
            "statusMessage": "Subindo atualizações pro GitHub..."
          }
        ]
      }
    ]
  }
}
